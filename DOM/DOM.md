# DOM
document
```bash
  document.addEvantListener('DOMContentLoaded',algunafuncion);//? `para pasarle una funcion solo cuuando el dom alla sido creado`
  
  document.getElementById()//?toma un elemento del html por su id
                           .textContent //?cambia e texto del contenido
                           .addEventListener('click',()=>{})//? vigila el evento que vamos a estar escuchando
                           .focus()//? hace focus al elemento
                           .removeAtribute()//?remueve un atributo de un elemento
                           .classList//? contiene funciones para manipular la lista de classes que tiene un elemento
                                      .add//? agrega una clase
                           .parentElement.removeChild(elhijoaRemover)//? remueve un elemento apollandoce en el padre
                           .remove()//? remueve un elemento
          .getElementByClass //?nombre de clases
          .getElementByTagName //?Etiquetas
          .bgColor = 'red' //?da color al body
          .forms[0].action//? puedo acceder a todos los formularios 
          .forms[0].campo.value
          .body.createElement('p');//?crea un elemento
          .body.appendchild(nuevoParrafo)//?
  
  
  
  
 
```


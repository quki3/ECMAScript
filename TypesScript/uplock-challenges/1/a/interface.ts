// describe a object that you will use after
interface User {
  name: String; // NOTE: use <;> instead of <,>
  id: number;
}
// now you can use the new interface by using sintax like <:typeName after a variable declaration
const user:User {
  name:"exem",
  id:0
};
